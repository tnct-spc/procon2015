#ifndef UTILS_HPP
#define UTILS_HPP
#include <vector>
#include <string>
// 文字列を文字列のデリミタにより分割する
std::vector<std::string> _split(std::string const & target, std::string const & delimiter);

#endif // UTILS_HPP
